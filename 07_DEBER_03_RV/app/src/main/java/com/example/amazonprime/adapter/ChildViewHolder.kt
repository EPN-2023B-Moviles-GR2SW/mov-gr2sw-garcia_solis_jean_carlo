package com.example.amazonprime.adapter

import android.view.View
import androidx.recyclerview.widget.RecyclerView
import com.example.amazonprime.databinding.ChildRvLayoutBinding

class ChildViewHolder(view: View) : RecyclerView.ViewHolder(view) {
    val binding = ChildRvLayoutBinding.bind(view)
}
